# Contributing

Design rules:
- Plans must be machine-verifiable (no narrative requirements).
- `VERIFIED` can only be written by `plansm verify` (and never by “explanations”).

Run:
```bash
go test ./...
```
